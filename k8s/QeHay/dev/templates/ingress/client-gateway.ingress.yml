apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qehay-ingress
spec:
  rules:
    - http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: client-gateway-dev
                port:
                  number: 3000
